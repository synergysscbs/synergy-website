<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: #000000;
            color: #fff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background:
                radial-gradient(circle at 20% 30%, rgba(30, 30, 40, 0.5) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(68, 204, 255, 0.1) 0%, transparent 40%);
            z-index: -1;
        }

        .container {
            max-width: 1000px;
            width: 100%;
            margin: 0 auto;
            padding: 2rem;
            flex: 1;
        }

        header {
            text-align: center;
            margin: 40px 0;
            padding: 30px;
            position: relative;
        }

        header::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, transparent, #44ccff, transparent);
        }

        h1 {
            font-size: 60px !important;
            font-weight: 600;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            margin-bottom: 15px;
            text-shadow: 0 2px 10px rgba(68, 204, 255, 0.5);
            font-weight: 700;
            background: linear-gradient(to right, #fff, #44ccff);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-family: "montserrat";
        }

        .subtitle {
            font-size: 1.5rem;
            opacity: 0.8;
            max-width: 600px;
            margin: 0 auto;
            font-weight: 450;
            letter-spacing: 0.05em;
        }

        .content {
            display: flex;
            flex-wrap: wrap;
            gap: 40px;
            margin-bottom: 40px;
        }

        .contact-info {
            flex: 1;
            min-width: 300px;
            background: rgba(15, 15, 20, 0.7);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .contact-info:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(68, 204, 255, 0.15);
        }

        .address-link {
            color: #fff;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            position: relative;
        }

        .address-link:hover {
            color: #44ccff;
            transform: translateY(-2px);
        }

        .address-link::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 100%;
            height: 1px;
            background: #44ccff;
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .address-link:hover::after {
            transform: scaleX(1);
        }

        .contact-form {
            flex: 1;
            min-width: 300px;
            background: rgba(15, 15, 20, 0.7);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .contact-form:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(68, 204, 255, 0.15);
        }

        .section-title {
            font-size: 1.5rem;
            margin-bottom: 25px;
            padding-bottom: 10px;
            border-bottom: 2px solid #44ccff;
            display: inline-block;
            font-weight: 600;
            text-shadow: 0 0 10px rgba(68, 204, 255, 0.3);
        }

        .info-item {
            margin-bottom: 25px;
            display: flex;
            align-items: flex-start;
            gap: 15px;
            transition: transform 0.3s ease;
        }

        .info-item:hover {
            transform: translateX(5px);
        }

        .icon {
            font-size: 1.2rem;
            color: #44ccff;
            margin-top: 3px;
            text-shadow: 0 0 10px rgba(68, 204, 255, 0.5);
        }

        .info-content h3 {
            margin-bottom: 5px;
            color: #44ccff;
            font-weight: 600;
        }

        .info-content p {
            opacity: 0.9;
            font-weight: 300;
        }

        .form-group {
            margin-bottom: 20px;
            position: relative;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #e0e0e0;
        }

        .required {
            color: #44ccff;
        }

        input,
        textarea {
            width: 100%;
            padding: 14px 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            background: rgba(10, 10, 15, 0.7);
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
            font-weight: 300;
        }

        input:focus,
        textarea:focus {
            outline: none;
            box-shadow: 0 0 0 2px rgba(68, 204, 255, 0.5);
            background: rgba(15, 15, 20, 0.8);
            border-color: rgba(68, 204, 255, 0.3);
        }

        textarea {
            min-height: 120px;
            resize: vertical;
        }

        .error {
            color: #ff6b6b;
            font-size: 0.85rem;
            margin-top: 5px;
            display: none;
            font-weight: 400;
        }

        .char-count {
            text-align: right;
            font-size: 0.85rem;
            margin-top: 5px;
            opacity: 0.7;
            font-weight: 300;
        }

        .char-count.short {
            color: #ff6b6b;
        }

        .submit-btn {
            background: linear-gradient(135deg, #1d4ed8, #44ccff);
            color: white;
            border: none;
            padding: 16px 32px;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(68, 204, 255, 0.2);
            margin: 20px auto 0;
            position: relative;
            overflow: hidden;
        }

        .submit-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }

        .submit-btn:hover {
            background: linear-gradient(135deg, #0b2590, #2c5364);
            box-shadow: 0 6px 20px rgba(68, 204, 255, 0.4);
            transform: translateY(-3px);
        }

        .submit-btn:hover::before {
            left: 100%;
        }

        .submit-btn:active {
            transform: translateY(1px);
        }

        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 15px 20px;
            border-radius: 8px;
            color: white;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.2);
            transform: translateX(100%);
            opacity: 0;
            transition: transform 0.5s ease, opacity 0.5s ease;
            z-index: 1000;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .notification.show {
            transform: translateX(0);
            opacity: 1;
        }

        .notification.success {
            background: rgba(16, 185, 129, 0.9);
        }

        .notification.error {
            background: rgba(239, 68, 68, 0.9);
        }

        .notification .close-btn {
            background: none;
            border: none;
            color: white;
            cursor: pointer;
            font-size: 1.2rem;
            margin-left: 10px;
            opacity: 0.8;
            transition: opacity 0.3s;
        }

        .notification .close-btn:hover {
            opacity: 1;
        }

        .progress-bar {
            position: absolute;
            bottom: 0;
            left: 0;
            height: 3px;
            background: rgba(255, 255, 255, 0.5);
            width: 100%;
            transform-origin: left;
        }

        .social-links a {
            color: #fff;
            font-size: 1.5rem;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            color: #44ccff;
            transform: translateY(-3px);
        }

        .copyright {
            opacity: 0.7;
            font-weight: 300;
        }

        @media (max-width: 768px) {
            .content {
                flex-direction: column;
            }

            h1 {
                font-size: 3rem;
                /* Adjusted for mobile */
                letter-spacing: 0.1em;
            }

            .navbar-nav {
                gap: 1rem;
            }

            .footer-nav {
                flex-wrap: wrap;
            }

            .submit-btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>

    <!-- Navbar placeholder - replace with your actual navbar -->
    <%- include('partials/navbar') %>

        <div class="container">
            <header>
                <h1>Contact Us</h1>
                <p class="subtitle">Get in touch with us. We'd love to hear from you!</p>
            </header>

            <div class="content">
                <div class="contact-info">
                    <h2 class="section-title">Contact Information</h2>

                    <div class="info-item">
                        <div class="icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="info-content">
                            <h3>Email</h3>
                            <p>synergy@sscbs.du.ac.in</p>
                        </div>
                    </div>

                    <div class="info-item">
                        <div class="icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div class="info-content">
                            <h3>Phone</h3>
                            <p>Kabir Upneja: +91-74281-06643</p>
                            <p>Kamya Gulyani: +91-93104-81081</p>
                        </div>
                    </div>

                    <div class="info-item">
                        <div class="icon">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="info-content">
                            <h3>Address</h3>
                            <p>
                                <a href="https://maps.app.goo.gl/Ak8qccUxMZgFDXk48" target="_blank"
                                    class="address-link">
                                    Dr. K.N. Katju Marg Rohini, Sector 16, PSP Area IV, New Delhi, Delhi 110089
                                    <i class="fas fa-external-link-alt" style="font-size: 0.8em; margin-left: 5px;"></i>
                                </a>
                            </p>
                        </div>
                    </div>

                    <div class="info-item">
                        <div class="icon">
                            <i class="fas fa-clock"></i>
                        </div>
                        <div class="info-content">
                            <h3>Response Time</h3>
                            <p>We typically respond within 24 hours</p>
                        </div>
                    </div>
                </div>

                <div class="contact-form">
                    <h2 class="section-title">Send us a Message</h2>

                    <!-- IMPORTANT: action & method added so the form posts to your /contact route -->
                    <form id="contactForm" action="/contact" method="POST">
                        <div class="form-group">
                            <label for="firstName">Your Name <span class="required">*</span></label>
                            <!-- name changed to firstName to match server validation -->
                            <input type="text" id="firstName" name="firstName" required>
                            <!-- server and client errors shown here -->
                            <div class="error" id="firstNameError" style="display:none;">
                                Please enter your name
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="email">Email Address <span class="required">*</span></label>
                            <input type="email" id="email" name="email" required>
                            <div class="error" id="emailError" style="display:none;">
                                Please enter a valid email address
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="phone">Phone Number</label>
                            <input type="tel" id="phone" name="phone">
                            <div class="error" id="phoneError" style="display:none;">
                                Please enter a valid phone number
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="message">Your Message <span class="required">*</span></label>
                            <textarea id="message" name="message" required minlength="10"></textarea>
                            <div class="char-count" id="charCount">0/10 characters</div>
                            <div class="error" id="messageError" style="display:none;">
                                Message must be at least 10 characters long
                            </div>
                        </div>

                        <button type="submit" class="submit-btn">
                            <i class="fas fa-paper-plane"></i> Send Message
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Fixed notification elements -->
        <div class="notification success" id="successNotification">
            <i class="fas fa-check-circle"></i>
            <span>Your message has been sent successfully!</span>
            <button class="close-btn">&times;</button>
            <div class="progress-bar"></div>
        </div>

        <div class="notification error" id="errorNotification">
            <i class="fas fa-exclamation-circle"></i>
            <span>Please fix the errors in the form.</span>
            <button class="close-btn">&times;</button>
            <div class="progress-bar"></div>
        </div>
        <!-- Flash + old form data -->
        <!-- Pass server data safely (no inline JS, CSP-friendly) -->
        <!-- Flash + old form data -->
        <div id="server-data"
            data-success="<%= (locals.success_msg && locals.success_msg.length) ? locals.success_msg[0] : '' %>"
            data-error="<%= (locals.error_msg && locals.error_msg.length) ? locals.error_msg[0] : '' %>"
            data-old='<%- JSON.stringify(oldData || {}) %>' data-errors='<%- JSON.stringify(errors || {}) %>'>
        </div>




        <%- include('partials/footer') %>


</body>

<script src="/javascript/contact.js"></script>


</html>